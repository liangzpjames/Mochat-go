# 主要修改说明

## 2026-07-19 SaaS 总后台获客前 MVP 重构

- 基于 Nuwa Admin 的 React、Vite、Tailwind、Radix 和 Lucide 交互体系重建总后台，保留 MoChat Go 现有后端、平台 RBAC、操作审计和生产证据接口，不引入 Nuwa 的模拟业务数据或独立后端。
- 将原连续堆叠的超前总后台收口为概览、客户租户、套餐、平台账号、系统运维和上线检查六个工作区；支付结算、客户成功、白标域名、服务账号和深度合规能力继续保留在后端但退出首版主导航。
- 新增 `/saas-admin/` 独立静态应用；原 `/dashboard/saasAdmin/page` 保持兼容并跳转到新入口。静态目录可通过 `MOCHAT_SAAS_ADMIN_DIST` 配置。
- 独立部署样例默认进入单人运营模式，确保唯一平台管理员可以完成首个客户开户；第二位平台管理员就位后应将 `MOCHAT_GO_SAAS_ADMIN_APPROVAL_REQUIRED` 改为 `1` 恢复双人审批。
- 上线边界和延期条件记录在 `docs/saas-admin-mvp-scope.md`；生产发布仍由六类真实外部证据门禁决定，不执行 24 小时本地运行。

## 2026-07-19 发布证据补证责任闭环

- 新增 `0097_saas_release_evidence_action_tracking`，为 MySQL 5.7 amd64、真实企业微信、真实微信开放平台、真实多租户、生产前端和目标环境短稳/外部监控六类生产证据分别保存负责人、截止时间、下一步动作、备注与乐观锁版本。
- 发布准备接口新增负责人目录、补证动作清单和已分配、未分配、逾期、临期、已完成汇总；补证状态根据证据复核结果、负责人和截止时间自动派生，不允许手工伪造为完成。
- 新增 `PUT /dashboard/saasAdmin/releaseEvidenceAction`。写入受 `platform.release.manage` 权限和平台管理租户边界保护，拒绝跨租户负责人、敏感信息、无负责人却携带截止时间以及陈旧版本覆盖，并记录独立平台操作审计。
- 补证动作独立于证据身份和候选快照：调整负责人或截止时间不会递增生产证据版本，也不会把已复核证据或候选误判为过期；证据通过后对应动作自动显示完成。
- 总后台交付工作区新增补证责任表和编辑器，支持按权限展示负责人、截止时间、下一步动作、逾期/临期与阻塞状态；目标环境稳定性默认采用短周期健康检查和外部监控证据，不要求 24 小时本地运行。
- 0097 迁移、真实 MariaDB/Redis 专项 smoke、迁移 rollback/replay/baseline/legacy、定向与全量 Go 测试、`go vet ./...` 和 `scripts/test.sh` 已通过；短周期证据包、独立交付包、预览升级与浏览器验收结果在本轮完成后回填。

## 2026-07-19 租户重新启用双人审批

- 新增 `0096_saas_tenant_enable_approval_guard` 和第 31 条 critical 动作 `tenant.enable`。审批开启时，平台管理员直接重新启用非平台业务租户返回 `428`，必须由两名不同复核人批准后才能恢复租户登录、旧 token 访问和订阅状态。
- 申请阶段冻结租户 ID、名称、停用状态，以及当前订阅 ID、状态和版本；批准执行时重新锁定租户和订阅并校验全部快照。任一状态、名称或订阅版本漂移均返回 `409`，同一审批可在恢复快照后重试。
- 租户状态、订阅恢复、平台操作审计和审批效果标记在同一 MySQL 事务提交。现有 `tenant.disable` 新申请也使用相同快照计划，执行器继续兼容 0096 以前已创建的旧版停用审批载荷。
- 总后台根据目标状态显示“提交停用审批”或“提交启用审批”；策略固定强制启用、零绕过阈值、2 名复核人、240 分钟 SLA、60 分钟提醒和 24 小时失效。
- 定向与全量 Go 测试、`scripts/test.sh`、96 版迁移 apply/checksum/down/replay/baseline/legacy、租户启停审批专项、审批治理、备份恢复和独立 Compose 均通过。最终本地证据包 `15/15`，六套短验收全绿，验收接入覆盖 `107/107`、manifest 路由 `224/224`、运行时唯一路径 `588/588`、运行时路由条目 730。
- 当前源码/build 指纹为 `ad95a7ba63dc2ec688355083c5cedbb9e81790ada25a4136ee01c01c06ae0e25`，纳入 950 个文件；0096 checksum 为 `9a5c47a06f395ef320399ad17100c47fdcae31fc372dd66fd5f588587ca1ed0b`。
- 保留数据卷的预览已升级到镜像 `sha256:45a45ca918529beeb0765ed5d91c1c79b0d913094d0cd4709dda61df201435c1`、迁移 `96/0096_saas_tenant_enable_approval_guard` 和 `31/31 critical`；App、MySQL、Redis 均 healthy。升级前 `0600` 快照为 `output/backups/mochat-preview-before-0096-tenant-enable-guard-20260719-061642.sql`，大小 680393 字节、SHA-256 `34f1c68d9483f51741d5955d85fb5cae5320e70ad3a4c2d2af49795f8e647e0a`。
- Playwright 在 `1440x1000` 和 `390x844` 验收“提交启用审批”与 `tenant.enable` 策略；页面无整体横向溢出，移动宽表只在内部滚动，控制台 0 error/0 warning，干净会话的 70 个动态请求全部为 `200`。临时 QA 租户及关联数据已清理，截图位于 `output/playwright/saas0096/`。
- 生产证据包于 `2026-07-19 07:41:29 CST` 刷新；严格门禁按预期未通过，唯一原因仍是六类外部生产证据缺失。发布准备保持 `0/6`、`ready=false`，本轮不执行 24 小时运行。

## 2026-07-19 租户域名新增双人审批

- 新增 `0095_saas_tenant_domain_create_guard` 和第 30 条 critical 动作 `tenant.domain.create`。审批开启时直接新增域名返回 `428`，必须由两名不同复核人批准后才创建待验证绑定；DNS TXT 所有权验证继续直接执行。
- 申请阶段只冻结标准化后的 `tenantId + hostname`，校验租户正常、域名未占用且租户未超过 10 个域名，不生成或保存 DNS 校验令牌。批准执行时重新锁定租户并复核状态、唯一性和配额，才生成 43 位令牌。
- 域名记录、初始路由/TLS 交付状态、平台操作审计和审批效果标记在同一 MySQL 事务提交。API 只在批准执行响应中交付一次 DNS 令牌，持久化审批结果删除 `verificationToken` 和 `verificationRecordValue`，仅保留 `verificationTokenDelivered=true`。
- 总后台“添加域名”会按策略显示“提交添加审批”，审批中心显示“新增租户域名绑定”；策略固定强制启用、零绕过阈值、2 名复核人、120 分钟 SLA、30 分钟提醒和 12 小时失效。
- 定向与全量 Go 测试、95 版迁移 apply/checksum/down/replay/baseline/legacy、域名新增与变更审批专项、原域名兼容、发布准备、备份恢复、审批主流程/治理和独立 Compose 均已通过。MySQL 5.7 实容器因当前 ARM 主机仅能取得 amd64 镜像而按既有规则跳过，不计作生产证据。
- 当前源码/build 指纹为 `432cc0978954d869c44b0cf6cc782ca0381758f2ab0297e32b3d990028a1f23d`，纳入 947 个文件；0095 checksum 为 `639cfdd943495ae04bcc100864258c71ae7f1e16f798c994b282ebaee572b6fd`。
- 最终本地证据包从 `2026-07-19 03:57:59 CST` 运行至 `05:22:22 CST`，结果 `15/15`；六套验收全绿，smoke 接入 `107/107`、manifest 路由 `224/224`、运行时唯一路径 `588/588`、运行时路由条目 730、前端 dist API `209/209`，源码稳定性门禁通过。
- 预览 App 已重建为镜像 `sha256:e1ddf1a69a13f4a3db6544ca3409fc87cfd43332c2a69b4cfa232c544b9e2677`，迁移账本为 `95/0095_saas_tenant_domain_create_guard`，30 条审批策略全部为 critical；App、MySQL、Redis 均 healthy，系统健康 `31/31`，纯 Go 路由 707 条，重建前后运行环境零差异。
- 升级前 `0600` 快照为 `output/backups/mochat-preview-before-0095-tenant-domain-create-guard-20260719-034908.sql`，大小 658052 字节、SHA-256 `6a171c22d397ae46577f63454c79ab7d9b91aa5ac87d8ec57347758776656073`。升级只应用 0095 并替换 App，原数据卷和业务数据均保留。
- Playwright 在 `1440x1000` 和 `390x844` 验收审批策略与租户域名中心；移动页面无整体横向溢出，审批宽表只在 368px 容器内滚动，控制台 0 error/0 warning，动态请求均为 `200`。截图位于 `output/playwright/saas0095/`，浏览器没有创建域名、提交审批或修改预览数据。
- 生产证据包于 `2026-07-19 05:24:47 CST` 刷新；严格门禁按预期返回失败，唯一原因是六类外部证据尚未提供。本轮不执行 24 小时运行；生产发布仍需 MySQL 5.7 amd64、真实企业微信、真实微信开放平台、两个以上真实 SaaS 租户、生产前端浏览器和目标环境短稳/外部监控。

## 2026-07-19 租户域名路由变更双人审批

- 新增 `0094_saas_tenant_domain_command_guard` 和 critical 动作 `tenant.domain.command`，覆盖主域名切换、域名启停、DNS 校验令牌轮换和域名删除。五类直接操作在审批开启时返回 `428`；DNS 所有权验证继续保留直接执行。
- 申请阶段冻结目标域名和租户全部未删除域名的完整路由快照；批准执行时按固定顺序锁定并重新校验。域名状态、路由/TLS 交付任务、平台操作审计和审批效果标记在同一 MySQL 事务提交，任一域名版本或主域状态漂移都会返回 `409`。
- DNS 校验令牌只在轮换审批批准执行时生成，不进入审批请求或持久化审批结果；审批执行失败不会泄露令牌，也不会留下部分业务副作用。
- 总后台域名操作会按策略显示“提交主域名/启用/停用/轮换/删除审批”，审批中心显示“变更租户域名路由”，策略固定为强制启用、零绕过阈值、2 名复核人、120 分钟 SLA、30 分钟提醒和 12 小时失效。
- 全量 Go 测试、`scripts/test.sh`、0094 迁移 apply/checksum/down/replay/baseline/legacy、域名审批专项真实 MariaDB/Redis smoke、原域名兼容 smoke、审批主流程/治理 smoke 和 MySQL 5.7 静态检查通过；验收接入覆盖为 `107/107`。完整本地证据包重建后 `15/15` 通过，其中 `core/saas/workers/cron/frontend/mysql57` 六套验收全绿。
- 同步修正独立 Compose 备份 smoke 仍断言 93 个迁移的滞后问题，将 `migrationCount` 期望值更新为 94；独立重跑和最终全量证据包均通过。
- 当前源码/build 指纹为 `111f573666625f1382b5908c9addefa56ac68ea16584a272a23ad95c1c339aa7`，纳入 945 个文件；0094 checksum 为 `a7f23b5c1d0c67abc18021bd20d7be68bea10c7f80dbc1943c0b680c0cb1ba1c`。
- 预览 App 已单独重建为镜像 `sha256:a6a28779d3f8567716e0b5342ff037af8bb581e663a740b3baef6ae4140e0053`，迁移账本为 `94/0094_saas_tenant_domain_command_guard`，29 条审批策略全部为 critical；App、MySQL、Redis 均 healthy，重建前后运行环境零差异。
- 升级前 `0600` 快照为 `output/backups/mochat-preview-before-0094-tenant-domain-command-guard-20260719-012928.sql`，大小 639408 字节、SHA-256 `ef006bae1e93c2ed2478419309e25469f56e826025f1a5c2390a2a1bb4714d06`。升级只应用 0094 并替换 App，原数据卷和业务数据均保留。
- Playwright 在 `1440x1000` 和 `390x844` 验收审批策略与租户域名中心；移动页面无整体横向溢出，审批宽表只在 368px 容器内滚动。截图位于 `output/playwright/saas0094/`，浏览器没有创建域名、提交审批或修改预览数据。
- 生产发布准备仍为 `0/6`、`ready=false`。还缺 MySQL 5.7 amd64、真实企业微信、真实微信开放平台、两个以上真实 SaaS 租户、生产前端浏览器和目标环境短稳/外部监控；本轮按要求未执行 24 小时运行。

## 2026-07-18 总后台权限感知工作区导航

- 将原先连续堆叠的 SaaS 总后台拆分为总览、租户套餐、运营、财务、通知、治理、安全和交付 8 个工作区，并保留全局筛选区。
- 工作区及模块下拉按平台 RBAC 动态显示；无权限工作区不进入键盘导航，也不会暴露对应模块入口。
- 增加工作区内模块跳转、左右方向键切换和本地状态持久化；修复权限数据异步加载时覆盖用户偏好工作区的问题。
- 移动端工作区标签使用局部横向滚动，并自动将当前标签定位到可见区域；页面自身在 `390x844` 下无横向溢出。
- 为原无障碍名称缺失的租户风险区补充“租户风险与额度”业务标签，避免模块下拉出现泛化编号。
- `go test ./internal/dashboard ./internal/server -count=1`、页面 JavaScript 语法检查、脚本语法检查和真实浏览器回归通过；控制台为 0 error/0 warning。
- 最终 App 镜像为 `sha256:a03582c70715c0ae1f8d508063885084950d2942fd6334e6e048b0c385d04ae9`，源码与验收指纹为 `2f3269d90df6bb3b62e030c9d1a599bf9b79ec7b87580da4099b2bc4d592d0ba`，纳入文件数为 `937`。
- 真实预览 API 复核为系统健康 `31/31`、备份运行 `2/2` 成功、恢复演练 `1/1` 成功；发布准备仍为 `0/6`、`ready=false`，本轮没有启动 24 小时运行。
- 浏览器证据位于 `output/playwright/saas-workspace-navigation/desktop-security-1440x900.png` 和 `output/playwright/saas-workspace-navigation/mobile-security-390x844.png`。

## 2026-07-18 预览环境灾备与安全闭环

- 将 `scripts/smoke_saas_backup_recovery.sh` 的迁移断言同步到 `0093/93`，完整灾备恢复 smoke 已通过。
- 为预览环境启用 5 分钟备份调度检查、专用恢复账号和恢复库自动创建/清理；运行配置保存在权限为 `0600` 的 `output/runtime/mochat-preview-app-runtime-20260718-181700.env`。
- 创建并校验加密备份 `bkp_20260718T095541Z_a87bef16cdc8.sql.gz.mgbk`，随后完成隔离恢复演练；迁移账本、表检查和清理均通过，未遗留恢复数据库。
- 将现有企业微信企业凭据从旧明文轮换为密文；通知、企业微信、微信开放平台和身份安全分别切换到独立密钥域并强制加密。
- 启用身份安全中心和清理定时任务，修复总后台四个身份安全接口因功能关闭返回 501 的问题；预览暂不强制持久会话，以保留现有登录兼容性。
- 当前 App 镜像为 `sha256:a03582c70715c0ae1f8d508063885084950d2942fd6334e6e048b0c385d04ae9`，源码与验收指纹为 `2f3269d90df6bb3b62e030c9d1a599bf9b79ec7b87580da4099b2bc4d592d0ba`，迁移账本为 `93/0093_saas_payment_settlement_resolve_guard`。
- 定向 Go 测试、独立交付包 smoke、灾备恢复 smoke、快速审计和桌面/移动浏览器回归均通过；平台健康为 `31/31`，浏览器控制台 0 error/0 warning。
- 发布准备仍为 `0/6`、`ready=false`。异地备份副本、生产 KMS/Secret Manager、强制持久会话与 MFA 迁移，以及六类真实外部生产证据仍需在目标环境补齐；本轮未启动 24 小时运行。

## 2026-07-11 独立 Go SaaS 版本

- 将兼容清单中的 `224/224` 路由全部由 Go 运行时接管，standalone 模式不依赖 PHP upstream、原 PHP 源码目录或外部 manifest。
- 保留原 `mc_` 业务数据模型并新增 `mochat_go_` SaaS 元数据、订阅、支付、退款、发票、结算、审批、权限、灾备、合规和身份安全表。
- 增加平台总后台、租户隔离、套餐额度、运营任务、告警通知、服务账号、加密备份、数据导出/擦除和 MFA/持久会话能力。
- 新增 `0054_saas_branding_profiles`：平台与租户品牌档案、同源素材约束、颜色/支持入口、乐观锁、RBAC 和审计。
- 新增 `0055_saas_tenant_domains`：租户自定义登录域名、DNS TXT 所有权校验、主域名、按 Host 绑定品牌与账号租户、RBAC 和审计。
- 新增 `0056_saas_domain_delivery`：持久化域名路由/TLS 交付状态、任务租约与重试、外部 Ingress/证书 Bridge、HMAC 签名回调幂等和总后台任务管理。数据库只保存非敏感证书引用，不保存证书 PEM 或私钥。
- 新增 `0057_saas_release_readiness`：把 MySQL 5.7 amd64、真实企微、真实微信开放平台、真实多租户、生产前端和稳定性六类证据纳入总后台，保存乐观锁复核记录与不可变候选快照；只有证据全部通过且源码 SHA-256 指纹一致时才生成 `ready` 候选。
- 新增 `0058_saas_release_evidence_integrity`：发布证据除 HTTPS 地址和源码指纹外，必须固化实际日志、报告或压缩包的 SHA-256 与字节大小；候选快照和审计同步保存工件身份，防止同一链接内容被替换后继续误判为可发布。
- 新增 `0059_saas_service_account_rate_limits`：服务账号支持每分钟和每日 OpenAPI 成功请求上限、并发安全的原子消费、标准 429/重试响应头、当日路由级成功与拒绝统计，以及总后台限额配置和实时用量展示。
- 新增 `0060_saas_service_account_usage_retention`：总后台支持 7/30/90 天 OpenAPI 趋势、账号和路由排行；合规策略可配置用量保留天数，维护命令和可选 cron 分批清理过期记录，并自动保护法律保留租户。
- 新增 `0061_saas_service_account_usage_alerts`：服务账号可配置日用量百分比、限流拒绝数和重复通知冷却；手动、维护命令和可选 cron 将预警统一写入 SaaS 告警、通知 outbox、任务账本与操作审计，条件恢复后自动解决并关闭尚未送达的旧通知。
- 新增 `0062_saas_audit_integrity`：按受影响租户为总后台操作日志建立 legacy 锚点和 SHA-256 结构摘要链，支持手动、维护命令与 cron 校验、篡改定位、保留期清理预估、读写分离 RBAC 和总后台治理页面；摘要链与校验历史纳入 157 项租户合规数据清单并按审计策略保留。
- 新增 `0063_saas_audit_anchor_signatures`：使用环境密钥为健康审计链创建 HMAC-SHA256 签名检查点，将规范化 JSON 证据写入独立存储，支持历史密钥环、孤儿证据/数据库回退检测、总后台创建与校验、维护命令、每日 cron 和系统健康探针；签名检查点纳入 158 项合规清单和 161 步可恢复擦除编排。
- 新增 `0064_saas_audit_anchor_remote_immutability`：把签名锚点复制到启用 Object Lock 的 S3 兼容异地桶，固化对象版本、SHA-256、大小、ETag、留存模式和到期时间；支持历史检查点分批自动回填、远端缺失/孤儿检测、删除保护验证、生产强制门禁和独立健康探针。
- 新增 `0065_saas_service_account_key_pepper_ring`：服务账号 API Key 改用独立 32 字节 pepper 密钥环并持久化 `hash_key_id`，不再与 Dashboard JWT 密钥共享生命周期；支持历史 pepper、旧 `legacy-jwt` Key 过渡、逐 Key 轮换、关闭兼容门禁、总后台风险概览和关键健康探针。
- 新增 `0066_saas_alert_credential_encryption`：租户通知 Webhook URL 与签名密钥改用 AES-256-GCM 密钥环加密落库，支持历史密钥兼容读取、存量明文批量轮换、独立加密强制门禁、总后台保护状态与关键健康探针。
- 新增 `0067_wecom_credential_encryption`：企业微信企业/应用 Secret、回调 Token/AES Key 和会话存档 Secret 改用独立 AES-256-GCM 密钥环加密落库，支持历史密钥读取、明文兼容迁移、平台 RBAC 轮换、脱敏审计和关键健康探针。
- 新增 `0068_wechat_open_credential_encryption`：微信开放平台组件 Ticket、组件认证资料、公众号授权码与刷新 Token 改用独立 AES-256-GCM 密钥环加密落库，支持历史密钥读取、明文兼容迁移、平台 RBAC 轮换、脱敏审计和关键健康探针。
- 新增 `0069_saas_release_candidate_approval`：发布候选门禁改为高风险审批动作，发布运营提交后需两名不同审批人会签；执行阶段重新远端校验六类生产证据，并把候选操作日志与审批副作用原子关联，阻止单人直接生成可发布候选。
- 新增 `0070_saas_approval_policy_change_guard`：审批策略的启停、会签人数、SLA、提醒和有效期变更改为固定双人会签；治理策略自身不能停用或降到单人，策略更新、审计日志和审批副作用标记原子提交，关闭单个业务审批也必须先经两名独立复核人通过。
- 新增 `0071_saas_backup_policy_change_guard`：平台数据库备份策略的启停、加密、异地副本、执行频率和保留参数改为固定双人会签；直接写接口返回 428，审批载荷冻结，策略更新、操作审计和审批副作用标记在同一事务提交。
- 新增 `0072_saas_backup_cleanup_saga`：备份保留清理改为双人会签后创建的持久化 Saga。审批请求冻结服务端计算的候选快照，执行按“异地副本、本地工件、数据库记录”顺序分步检查点推进，失败可续跑且清理中的备份禁止校验、复制和恢复。
- 新增 `0073_saas_compliance_export_deletion_saga`：合规导出提前删除改为不可关闭的双人会签，审批冻结导出与工件摘要，执行按“加密工件、导出台账”顺序保存检查点；法律保留和未终结擦除请求会阻断删除，失败任务支持人工或维护任务续跑。
- 新增 `0074_saas_compliance_legal_hold_release_guard`：解除法律保留改为不可关闭的双人会签，审批冻结保留编号、租户、原因、起止时间、版本与解除依据；执行时在同一数据库事务内校验快照、解除保留、写操作审计并标记审批副作用，阻止单个管理员解锁导出删除和租户擦除门禁。
- 新增 `0075_saas_compliance_policy_change_guard`：租户数据合规生命周期策略变更改为不可关闭的双人会签，覆盖导出、擦除、账务、审计和服务账号用量保留边界；直接写接口返回 428，审批申请校验并冻结完整策略与当前版本，执行时将策略更新、操作审计和审批副作用标记原子提交。
- 新增 `0076_saas_identity_policy_change_guard`：租户身份安全策略变更改为不可关闭的双人会签，覆盖密码失败锁定、会话、MFA、登录 IP 白名单和身份数据保留边界；直接写接口返回 428，审批申请校验并冻结标准化策略与当前版本，执行时将策略更新、操作审计和审批副作用标记原子提交。
- 新增 `0077_saas_tenant_disable_approval_guard`：critical 级租户停用从单人审批提升为不可关闭的双人会签；总后台治理行固定启用且最低两票，直接停用仍由既有 428 门禁阻断，审批执行继续复用租户状态、订阅同步、操作审计和审批副作用同事务提交链路。
- 新增 `0078_saas_critical_approval_policy_guard`：13 条 critical 策略统一强制启用、零绕过阈值和至少双人会签，退款升级为全额双人审批；后端对历史不安全配置做防御性钳制，总后台改由服务端治理元数据驱动，不再维护动作白名单。
- 新增 `0079_saas_service_account_key_revoke_guard`：服务账号 API Key 吊销升级为不可绕过的 critical 双人审批；申请冻结服务账号、Key 与版本，直接吊销返回 `428`，执行时将 Key 失效、操作审计和审批效果标记在同一事务提交。
- 新增 `0080_saas_service_account_update_guard`：服务账号配置变更升级为不可绕过的 critical 双人审批；申请冻结账号状态、作用域、CIDR、额度、预警、有效期与当前版本，直接更新返回 `428`，执行时将账号更新、操作审计和审批效果标记在同一事务提交。
- 新增 `0081_saas_service_account_key_rotate_guard`：服务账号 API Key 轮换升级为不可绕过的 critical 双人审批；申请只冻结账号版本与轮换参数，批准执行时才生成新 Key，并将轮换、操作审计和审批效果标记在同一事务提交；明文只通过执行响应交付一次，审批持久化结果不保存明文。
- 新增 `0082_saas_service_account_create_guard`：服务账号创建升级为不可绕过的 critical 双人审批；申请冻结标准化账号配置、初始 Key 名称与有效期，但不生成密钥；批准执行时才生成首个 Key，并将账号、Key、操作审计和审批效果标记原子提交。明文只在执行响应显示一次，审批持久化结果不保存明文。
- 新增 `0083_saas_identity_mfa_reset_guard`：平台重置用户 MFA 升级为不可绕过的 critical 双人审批；申请冻结目标用户、租户、MFA 凭据版本和标准化原因，批准执行时原子禁用 MFA、清除动态码密钥与恢复码、失效挑战、撤销全部活动会话、写安全事件与操作审计并标记审批效果，阻止单人降低账号二次认证强度。
- 新增 `0084_saas_package_definition_guard`：平台套餐创建、启停、说明与全部额度变更升级为不可绕过的 critical 双人审批；套餐增加乐观锁版本，申请冻结当前定义、目标定义和租户影响，批准执行时原子更新套餐、写操作审计并标记审批效果。
- 新增 `0085_saas_tenant_package_assignment_guard`：平台租户套餐分配升级为不可绕过的 critical 双人审批；租户套餐绑定增加独立版本，申请冻结租户状态、当前绑定、目标套餐定义版本和额度影响，批准执行时原子更新绑定、同步订阅、写操作审计并标记审批效果，续费、开户和套餐同步会递增绑定版本以阻止陈旧审批覆盖新状态。
- 新增 `0086_saas_tenant_provision_approval_guard`：平台开户升级为不可绕过的 critical 双人审批；申请冻结套餐定义版本、开户预览、任务版本和任务请求摘要，管理员密码只保存 PHP 兼容 bcrypt 哈希并在 API 中脱敏；批准执行时原子创建租户、管理员、角色菜单、套餐、订阅和开通记录，同时提交任务状态、操作审计和审批效果标记。
- 新增 `0087_saas_tenant_renewal_approval_guard`：租户续费升级为不可绕过的 critical 双人审批；直接续费、单任务直接应用和批量直接应用返回 `428`，申请冻结租户、套餐绑定、目标套餐、订阅和可选任务快照，批准执行时原子提交套餐权益、续费账单、订阅、任务状态、操作审计与审批效果标记。
- 新增 `0088_saas_subscription_transition_approval_guard`：租户订阅状态迁移升级为不可绕过的 critical 双人审批；直接迁移返回 `428`，申请冻结租户状态、订阅 ID 与版本、目标状态、周期字段和取消标记，批准执行时原子提交订阅状态、订阅事件、操作审计与审批效果标记，并拒绝平台租户、租户状态漂移和订阅版本漂移。
- 新增 `0089_saas_invoice_issue_approval_guard`：蓝票或红票正式开具升级为不可绕过的 critical 双人审批；直接推进到 `issued` 返回 `428`，申请冻结单据身份、状态、版本、票面信息以及支付订单版本和金额台账，批准执行时原子更新单据、开票/红冲金额、操作审计与审批效果标记，并拒绝单据或订单漂移。失败、取消和进入处理中仍保留直接纠错链路。
- 新增 `0090_saas_payment_order_create_approval_guard`：创建收款订单升级为不可绕过的 critical 双人审批；直接创建返回 `428`，申请冻结租户状态、套餐定义版本、完整额度、金额与服务周期，批准执行时原子创建订单、写操作审计并标记审批效果。订单持久化套餐版本和额度快照，后续回款按成交快照开通权益；租户或套餐在审批期间漂移会拒绝执行。
- 新增 `0091_saas_payment_settlement_close_guard`：关闭支付渠道结算批次升级为不可绕过的 critical 双人审批；直接关闭返回 `428`，申请冻结批次身份、账期、渠道、币种、来源摘要、对账计数、金额台账和版本，批准执行时重新锁定并逐项校验，在同一事务内关闭批次、写操作审计并标记审批效果。未解决差异或审批期间任一冻结字段漂移都会拒绝执行。
- 新增 `0092_saas_payment_settlement_reopen_guard`：重开已关闭结算批次升级为独立的 critical 双人审批；直接重开返回 `428`，申请使用版本化载荷冻结完整批次账务、导入/对账信息和关账人、关账时间、关账原因，批准执行时重新锁定并逐项校验，再原子恢复对账状态、写操作审计并标记审批效果。历史 0091 关账审批载荷继续兼容执行。
- 新增 `0093_saas_payment_settlement_resolve_guard`：解决、忽略或重新打开渠道结算差异升级为不可绕过的 critical 双人审批；直接处理返回 `428`，申请在一致性事务中冻结完整差异条目与所属批次账务，批准执行时按固定锁序逐项复核，再原子更新条目、批次计数、操作审计和审批效果。任一账务、处理审计或无版本漂移都会拒绝执行。
- 新增 `0094_saas_tenant_domain_command_guard`：主域名切换、域名启停、DNS 校验令牌轮换和域名删除升级为不可绕过的 critical 双人审批；申请冻结目标域名与租户完整路由快照，批准执行时重新锁定并校验全部域名版本，再原子提交域名状态、交付任务、操作审计和审批效果。审批等待期间任何主域名或候选路由变化都会拒绝旧申请。
- 身份登录与服务账号 OpenAPI 共用受信代理 CIDR 客户端 IP 解析器；只有 TCP 对端命中显式可信网段时才采信 `X-Forwarded-For`/`X-Real-IP`，总后台同步展示当前解析模式。
- 发布镜像在构建阶段计算源码与验收配置指纹并写入 Go 二进制；总后台只能以当前运行版本的权威指纹复核证据和生成候选，拒绝客户端伪造或覆盖指纹，未内置指纹时门禁直接禁用。
- 总后台灾备中心增加自动备份调度启用状态、扫描间隔和启动检查状态；系统健康中心增加关键级 `backup_automation` 探针，阻止把仅能手工备份但没有自动调度的环境误判为灾备就绪。
- 统一 standalone 应用、MariaDB 与运维命令的业务时区；MySQL 连接池会按 DSN `loc` 设置每条会话的 `time_zone`，修复跨 UTC/本地日期边界时日报、审计和通知窗口漏数。
- 安全登录页和 dashboard 租户元数据改为读取本地品牌档案；移除对 `api.mo.chat`、`oss.mo.chat` 和旧 MoChat 前端框架 CDN 的运行时依赖。
- Vue 2.6.10、Vue Router 3.1.3、Vuex 3.1.1 和 Axios 0.19.0 改为随独立包同源提供；移除上游 dashboard 的百度统计注入。
- 增加空库迁移、rollback/baseline/legacy、MySQL 5.7、RBAC、真实 MariaDB/Redis、浏览器和独立包验收脚本。

详细阶段记录和验证证据见 `docs/standalone-gap.md` 与 `docs/evidence/`。
