# 消息拦截与关键词库实施计划

1. 新增 0112 可逆迁移，建立词库、版本、规则、记录和审计表，并更新迁移基线测试。
2. 定义领域模型、校验、Provider 接口和纯匹配器，先用测试锁定发布版本与决定语义。
3. 实现 MySQL Provider：词库/词条维护、事务发布、规则维护、消息评估、记录查询和审计。
4. 注册 `/dashboard/keyword-library/*` 与 `/dashboard/message-intercept/*` 路由，沿用登录、企业范围和 RBAC 解析。
5. 实现两个 React 页面，替换通用未接入页面并覆盖查询、维护、发布、评估记录和复核交互。
6. 运行 Go、前端和构建测试；对当前 MariaDB 增量应用 0112，重新构建现有 Compose 服务，不删除数据卷。
7. 使用浏览器分别验证两个页面，保存截图与验收报告，更新 Phase 3.3 状态并提交当前分支。
