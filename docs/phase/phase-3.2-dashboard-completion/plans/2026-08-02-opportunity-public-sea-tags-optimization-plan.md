# 商机、公海与客户标签页面优化实施记录

## 目标

在不扩展后端契约的前提下，将三个页面统一到 phase3.2 已验收的 SCRM 视觉与交互基线。

## 实施项

- [x] 查看圆弧商机、公海、客户标签实际页面并记录字段和操作入口。
- [x] 为三页统一页头、数据概览和刷新工具栏补充失败测试。
- [x] 完成商机与公海页面信息层级、列表工具栏和共享样式优化。
- [x] 完成企业标签导航、标签目录工具栏和不支持能力的明确禁用状态。
- [x] 运行三页目标测试与类型检查。
- [x] 运行 Dashboard 全量测试和生产构建。
- [x] 保留数据卷重建 `mochat-go-desktop` 并完成浏览器验收。
- [x] 独立代码审查并提交当前 phase3.2 分支。

## 部署环境修复

浏览器验收发现保留数据卷缺少 0109 标签目录结构，导致客户标签接口返回服务不可用。已安全应用幂等的 `0109_scrm_customer_tag_parity`，创建标签组表并为标签补充 `group_id`，随后标签页面恢复为正常空数据状态。未清空或重建数据卷。
