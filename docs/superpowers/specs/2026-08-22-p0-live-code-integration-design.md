# Mochat 今日 P0 活码集成与门禁修复设计

> 日期：2026-08-22  
> 状态：已获用户授权按推荐方案连续实施，完成后统一审阅  
> 初始基线：`origin/main=b9a47cab45ec872bc81e61a20b06a8a3529311b2`；实施中按用户指令同步 `origin/main=43c781514a4549e806517ab2b2a707f1320e2df7`
> 实施分支：`feat/p0-live-code-integration-20260822`

## 1. 目标与范围

本任务把渠道活码与群活码收敛为 Mochat 内唯一、可运行、可审计的专用工作区，同时修复 Dashboard RBAC catalog 与迁移 seed 不一致、圆弧 benchmark 阶段值非法两项 P0 门禁。交付包含设计、实施计划、代码、迁移、测试、Docker 三视口浏览器验收和中文自测报告。

本期不修改 Phase 7，不接入真实企微会话存档，不删除或提交用户当前工作树的受保护未跟踪内容。实施中用户明确要求渠道活码和群活码页面以另一会话已经完成的优化为准，因此本分支同步 `origin/main@43c7815` 的 `0152_group_code_direct_join`、群直连 Provider 适配和前端交互，不再自行修改两类活码页面；本任务继续负责迁移改号兼容、RBAC、benchmark 与完整验收。

## 2. 现状、差异与根因

### 2.1 Git 与迁移现状

- 用户当前工作树与远端已继续演进到 `main/origin/main=43c7815`；本任务仍只在隔离 worktree 开发。
- 隔离 worktree 精确从 `b9a47cab` 创建，不包含当前脏树的修改。
- 远端迁移 0140—0151 各编号唯一；0150 为关键词快照修复，0151 为 AI 洞察 API 资源。
- `0152_group_code_direct_join.{up,down}.sql` 已由另一会话提交并进入 `origin/main@43c7815`。本任务将活码 workspace 迁移保持为 `0153_live_code_workspace`，RBAC 对账保持为 `0154_dashboard_permission_resource_reconciliation`；最终顺序为 0150 关键词修复、0151 AI 洞察资源、0152 群直连、0153 活码 workspace、0154 RBAC 对账，编号与版本名均唯一。

### 2.2 活码实现差异

远端基线仍由通用营销页承载渠道/群活码，数据字段、筛选和表单业务化不足。本地有效提交已经提供生命周期字段、统计、导出、作废、群活码分组、事件账本和专用前端页面，但迁移占用冲突的 0150，且早期创建表单暴露员工 ID、标签 ID 与群 JSON。

`a28dd4b` 与后续对齐提交提供了有价值的抽屉层级、关闭按钮、遮罩关闭和响应式样式；`b01f2d1d` 已把员工与群聊改为真实接口驱动的选择器。最终实现吸收这些交互，但只保留 `LiveCodeWorkspacePage` 一个权威页面入口，不并存通用页和专用页两套活码实现。

### 2.3 两项门禁根因

`check:phase4-dashboard-page-rbac` 当前用 0127、0131—0133 构造 migration seed，却未纳入 0141—0151 的资源增量和停用语义。实测旧构造为 250 条、catalog 为 286 条：catalog 多 40 条，旧 seed 多 4 条。其中 33 条已有后续迁移，7 条客户继承资源从未被补种；4 条旧资源中，文件录音写资源已由 0145 停用，导出旧资源仍需正式停用。

`/chat/file-audio` 的实现目录、页面文档和现有阶段体系均属于 Phase 3.5，但 manifest 写成校验器从未定义的 `3-final`。正确修复是改为 `3.5`，不扩大允许集合。

## 3. 圆弧 AI 可复验调研证据

调研日期为 2026-08-22，使用用户已登录的应用内浏览器，只进行页面导航、打开抽屉/详情和空字段校验，没有提交、删除、导出或下载。

| 路由/状态 | 可复验观察 | 本任务采用方式 |
| --- | --- | --- |
| `#/acquisition/v2-channel-code` 列表 | 左侧默认分组；名称、创建人、关联员工筛选；添加、统计、刷新；表格列为二维码/名称、创建人/日期、人员/部门、标签、有效期、新增好友数；空态“暂无数据”；底部批量操作与分页 | 借鉴信息顺序和紧凑工具栏；Mochat 不复制圆弧刷新重复语义，保留明确查询/重置与操作后刷新 |
| 渠道活码新增 | 三步“基础设置→接待方式→设置海报”；基础设置含分组、名称、标签、客户备注、欢迎语、附件、有效期；右侧手机预览 | P0 使用真实员工选择、业务字段和二维码状态；没有后端支撑的海报/附件能力显示未接入，不伪造 |
| 渠道空字段下一步 | 字段下显示“请输入活码名称”，同时出现“请检查填写是否合规”告警 | Mochat 采用字段级错误+抽屉级错误摘要，聚焦首个错误 |
| `#/acquisition/group-code` 列表 | “无限群活码/自建群活码”页签；名称、创建人、日期筛选；真实列表有查看、编辑、复制、删除；底部批量操作与分页 | 本期权威页至少覆盖当前真实 `workRoomAutoPull` 模式；不把未实现的第二数据域伪装为可用页签 |
| 群活码详情 | 居中详情层，左侧二维码与下载/修改操作，右侧基本设置 | Mochat 复用自身右侧抽屉语言，提供真实二维码、基础配置和数据口径，不复制下载能力假按钮 |
| 群活码新增/编辑 | 两步“基础设置→设置海报”；最多 5 群、有序群聊、满 200 人后自动建群、新群名称和起始序号；空字段有逐项错误与总告警 | P0 使用真实群聊选择器并限制最多 5 项；仅提交当前后端支持字段，未接入自动建群时明确不可用 |
| 390×844 群新增 | 固定宽度抽屉横向溢出，截图只剩右侧预览列，主表单不可见 | 该表现记录为反例；Mochat 移动端必须单列、抽屉占满宽度、内容纵向滚动、操作区固定且无横向溢出 |

圆弧页面的记录数量、人员姓名、二维码和统计值仅用于确认控件存在，不进入 Mochat 代码、测试夹具或文档示例数据。

## 4. 方案比较与权威选择

### 方案 A：整体 merge 分叉 `main`

可以快速得到活码代码，但会带入大量与远端反向删除、旧阶段实现和监督台账修改，并重现 0150 冲突。不可控，拒绝。

### 方案 B：语义重放有效提交并补齐门禁（采用）

先逐个吸收 6 个有效活码提交并把迁移改号到 0153；随后按用户最新指令把 `origin/main@43c7815` 已完成的选择交互、群直连能力及页面实现整体作为权威版本同步进来，不再叠加本分支的页面改造。通过 0154 对账迁移与校验器 overlay 恢复 RBAC 单一合同。该方案改动可追踪、回滚边界清晰，且不覆盖用户当前树。

### 方案 C：从远端重新实现全部能力

能够得到最整洁历史，但会重复已经过测试的后端、存储和页面工作，扩大遗漏风险。本期拒绝。

权威选择为方案 B。路由注册、benchmark registry 和菜单只指向专用活码页；旧通用组件不再承担这两个路由。

## 5. 数据、接口与真实性

| 页面能力 | 前端请求 | 后端/存储事实源 | 空值/错误语义 |
| --- | --- | --- | --- |
| 渠道列表 | `GET /channelCode/index` | `mc_channel_code`、员工/部门关联、真实联系人归因 | 统计未接入返回 `null/available=false`，不显示为 0 |
| 渠道统计/导出 | `GET /channelCode/workspaceStatistics*`、`GET /channelCode/export` | 渠道联系人事件与服务端重新查询 | 返回 `asOf` 与口径；导出失败保留当前页面 |
| 渠道创建/编辑 | `POST /channelCode/store`、`PUT /channelCode/update` | 既有 channelCode + 企微 Provider | Provider 失败不生成占位二维码，表单保留且展示错误 |
| 渠道作废 | `POST /channelCode/batchInvalidate` | 生命周期字段与企微同步结果 | 部分失败逐条反馈；不物理删除 |
| 群列表/详情 | `GET /workRoomAutoPull/index|show` | `mc_work_room_auto_pull` 与当前企业群聊 | 无可验证入群统计时显示未知态 |
| 群创建与直连 | `POST /workRoomAutoPull/store` | `0152` 提供的 Join Way 生成链路；员工、群聊选项来自真实业务接口 | 缺少实体、权限或 Provider 时禁止提交并给出明确原因；页面行为以 `43c7815` 为权威 |
| 群分组 | 本期无可注册的 `/groupCodeGroup/*` Handler | `mc_group_code_group` 仅保留兼容表结构 | 不在目录、RBAC 或界面暴露未注册能力；后续实现 Handler 后另行启用 |

前端不得写生产假数据、猜测人员名称、伪造二维码或用成员存量冒充扫码归因。所有查询、更新、统计和导出按当前租户、企业与数据权限执行。

## 6. 数据库、迁移与 RBAC

`0153_live_code_workspace` 包含渠道生命周期、Provider 状态、数据来源、群活码分组、活码事件账本、索引及活码资源 seed。字段、索引与表均采用 `IF NOT EXISTS` 增量补齐，使已经误应用旧 `0150_live_code_workspace` 的开发环境和全新环境都能安全升级；down 会检查迁移台账，若存在旧版本事实则保留其拥有的数据结构与资源，否则删除 0153 新建的资源、表、索引与字段。所有测试、文档和迁移顺序引用统一改为 0153。

`0154_dashboard_permission_resource_reconciliation` 以 catalog 当前合同为输入语义：幂等补种 catalog 所需资源，修正两条租户级扫描状态资源的 scope，并停用 catalog 已删除的旧导出与文件录音写入资源。同步新基线后，渠道更新路由继续沿用 0153 的正式授权；客户继承页所需的 8 条读取/写入资源统一由 0154 补齐，down 只撤回这 8 条首次引入资源并恢复此前仍活跃的两条旧导出资源；0145 已停用的媒体写资源不被错误恢复。校验器读取该迁移的 additions/deactivations overlay，最终 seed 必须与 catalog 逐条、逐 scope 精确相等。

资源同时满足三层合同：前端真实 API 使用、Go 生产路由注册、catalog 所属页面。新增接口若不能通过三方交叉检查，不得以 exemptions 放行。

## 7. 交互状态机

### 7.1 列表

`idle → loading → success(empty|rows)`；查询失败进入 `error` 并保留上一次成功列表。筛选草稿只有点击查询才生效；重置清空筛选、分组和页码。翻页、创建/编辑成功和重试触发真实请求。

### 7.2 创建抽屉

`closed → loading-options → editing → validating → saving → success|save-error`。原计划中的独立编辑增强已被用户指定的 `43c7815` 权威页面替换，本分支不再改动其交互范围。

- 创建打开时加载当前权限范围内的员工/群聊。
- 关闭按钮、遮罩和取消统一触发关闭；saving 时禁止关闭和重复提交。
- 校验失败显示字段错误与总摘要，不发请求。
- 保存失败保留输入和选择；成功关闭抽屉、刷新列表并显示成功反馈。
- 选择器采用草稿/确认两阶段；取消不污染已确认选择；群聊最多 5 个并保持顺序。

### 7.3 详情与危险操作

详情 `closed ↔ open`，支持关闭按钮、遮罩和 Escape。作废/删除必须进入确认态；仅在用户确认后写请求。真实 Provider 错误与本地状态分开显示。

## 8. 响应式方案

- ≥1440px：分组栏 220—240px，主区自适应；2560×1440 不把内容限制在窄列。
- 768—1439px：筛选器自动换行，表格容器横向滚动，操作区不覆盖内容。
- <768px：分组栏折叠为顶部选择；表格改为可读卡片或受控横向滚动；抽屉占满视口宽度并单列；预览移到表单下方；底部操作区 sticky；触控目标至少 40px。
- 390×844 必须能看到名称、员工/群聊选择、错误和保存/取消，页面本身不得出现异常横向溢出。

## 9. 兼容、发布与回滚

后端和迁移先于新前端发布，新增字段均提供兼容默认值。旧记录保留，`data_source` 与 Provider 状态决定是否进入业务展示。前端回滚不删除 0153 数据；后端回滚前先停用新页面和生命周期任务，再依次执行 0154、0153 down。若台账存在历史 `0150_live_code_workspace`，0153 down 只撤销自身版本事实并保留旧迁移拥有的 schema，避免误删共享字段和业务数据；RBAC 对账 down 只恢复可确定的前态，不猜测用户自定义权限。

Docker 只重建必要服务，禁止 `down -v`、`down --volumes` 或删除命名卷。若无 `MOCHAT_GO_MYSQL_INTEGRATION_DSN`，真实 MariaDB integration 必须报告 SKIP，不能写成 PASS。

## 10. 验收标准

1. 两个路由只有一套权威页面；活码页面行为和测试以用户指定同步的 `43c7815` 为准，本分支不再叠加重复实现。
2. 0150、0151 保持远端语义，活码使用 0153，RBAC 对账使用 0154；所有引用一致，无重复编号。
3. catalog、前端 API、Go 注册路由和迁移最终资源 seed 精确一致，RBAC 门禁通过。
4. `/chat/file-audio` phase 为真实 `3.5`，benchmark 门禁通过且校验器未放宽。
5. 新增行为具备 RED→GREEN 证据；Dashboard 全量测试、typecheck、production build、相关 Go 包及用户指定全部检查为绿。
6. Docker 中 app、MySQL、Redis 健康，命名卷保留；2560×1440、1440×900、390×844 完成真实页面验收，控制台和关键请求无未解释错误。
7. 最终中文报告列出提交、PASS/FAIL/SKIP、真实 MariaDB DSN 状态、外部凭证阻塞和回滚点；不修改 `docs/PROJECT_PROGRESS.zh-CN.md`。
