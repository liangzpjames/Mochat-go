# AI 设置编辑器与知识库上传增量实施计划

日期：2026-08-24

> 按 `writing-plans` 与测试驱动要求执行：先用失败测试固定行为，再写最小实现，并在完成声明前运行新鲜验证。

## 目标文件

- `web/apps/dashboard/src/features/ai-settings/agent-page.tsx`
- `web/apps/dashboard/src/features/ai-settings/knowledge-base-page.tsx`
- `web/apps/dashboard/src/features/ai-settings/ai-settings-pages.test.tsx`
- `web/apps/dashboard/src/styles/index.css`
- `web/apps/dashboard/src/styles/ai-settings-layout.test.ts`
- `docs/verification/2026-08-24-ai-settings-dual-assistants-final-report.zh-CN.md`

本增量不需要数据库迁移和新 API；沿用已有知识库创建、文档上传及两类助手保存接口。

## 任务 1：用测试固定助手横向布局

- [ ] 在页面测试中断言两个编辑器使用专用横向表单与区块 class。
- [ ] 在布局契约测试中断言桌面十二列网格、三块四列、提示词双列和窄屏回落规则。
- [ ] 单独运行相关测试，确认因生产代码尚未实现而失败。

## 任务 2：用测试固定知识库可选上传合同

- [ ] 覆盖不选文件：只创建知识库，不上传。
- [ ] 覆盖选择文件：创建成功后以返回的知识库 ID 上传。
- [ ] 覆盖上传失败：知识库保留，弹窗结束并显示可重试的部分失败反馈。
- [ ] 覆盖仅选择文件时的未保存关闭确认。
- [ ] 单独运行相关测试，确认红灯原因与预期一致。

## 任务 3：实现助手横向弹窗

- [ ] 将两个编辑弹窗宽度调整为宽屏友好的响应式值。
- [ ] 为基础信息、知识库、会话范围/规则和提示词添加语义布局 class。
- [ ] 在样式中实现桌面三栏、提示词双栏、中宽两栏和窄屏单栏。
- [ ] 保持字段、接口、校验和保存状态不变。

## 任务 4：实现新建知识库可选文档

- [ ] 增加仅用于新建流程的可选文件状态和选择控件。
- [ ] 把文件选择纳入脏状态与重置逻辑。
- [ ] 在同一保存 mutation 中顺序调用创建与上传接口。
- [ ] 区分创建失败、完整成功、上传失败和解析失败反馈。
- [ ] 保持编辑知识库和“管理文档”现有流程不变。

## 任务 5：自动化与运行时验收

- [ ] 运行相关 Vitest，确认新增用例转绿。
- [ ] 运行 Dashboard 全量测试、typecheck、范围 lint 和 production build。
- [ ] 运行相关 Go 包测试、RBAC/benchmark/evidence 门禁与 `git diff --check`。
- [ ] 使用保留卷 Docker 重建应用并确认健康。
- [ ] 使用真实登录态验证两个助手编辑弹窗桌面/窄屏布局。
- [ ] 浏览器验证知识库无文件创建、有文件创建上传、刷新持久化和清理测试数据。
- [ ] 更新中文实施与自测报告，记录命令、结果、证据和已知限制。

## 回滚边界

- 代码回滚只需撤销本增量前端组件、样式与测试提交；无数据库结构回滚。
- 浏览器验收产生的临时知识库和文档通过正常产品删除入口清理，不直接修改业务表或删除 Docker 卷。
- 上传部分失败时不自动删除知识库，避免破坏用户已成功创建的数据。
