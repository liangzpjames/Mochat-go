import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{render,screen,waitFor}from'@testing-library/react';import{MemoryRouter}from'react-router';import type{ReactNode}from'react';import{expect,it,vi}from'vitest';import{DashboardAccessProvider}from'../../app/access-context';import type{AccessContext}from'../../app/access-loader';import{RefuseArchivePage,SilentCustomerPage}from'./phase33-closure-pages';
const access:AccessContext={session:{token:'t',userId:'1',corpId:'7',expiresAt:null},corp:{id:'7',name:'企业',authorized:true},menu:[],allowedRoutes:new Set(),allowedActions:new Set()};const wrap=(n:ReactNode)=><MemoryRouter><QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><DashboardAccessProvider value={access}>{n}</DashboardAccessProvider></QueryClientProvider></MemoryRouter>;
it('loads silent records with rule tab',async()=>{const read=vi.fn().mockResolvedValue({items:[]});render(wrap(<SilentCustomerPage api={{read,write:vi.fn()}}/>));await waitFor(()=>expect(read).toHaveBeenCalledWith('/silent-customer/records',{page:1,perPage:50}));expect(screen.getByRole('button',{name:'规则配置'})).toBeTruthy();expect(screen.queryByText('数据提供方未接入')).toBeNull()});
it('loads refuse archive records',async()=>{const read=vi.fn().mockResolvedValue({items:[]});render(wrap(<RefuseArchivePage api={{read,write:vi.fn()}}/>));await waitFor(()=>expect(read).toHaveBeenCalledWith('/refuse-archive/records',{page:1,perPage:50,authorizationStatus:'refused'}));expect(screen.getByRole('heading',{name:'拒绝存档'})).toBeTruthy();expect(screen.queryByText('数据提供方未接入')).toBeNull()});
