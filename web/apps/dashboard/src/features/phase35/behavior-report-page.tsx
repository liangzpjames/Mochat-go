import {useQuery} from '@tanstack/react-query';import type {Phase35Api} from './api';import {ReportPrimitives} from './report-primitives';export function BehaviorReportPage({api}:{api:Phase35Api}){const q=useQuery({queryKey:['p35-report','behavior'],queryFn:()=>api.read('/dashboard/reports/behavior',{corpId:1,timezone:'Asia/Shanghai',startAt:'2026-01-01T00:00:00Z',endAt:'2027-01-01T00:00:00Z'})});return <section className="dashboard-content"><h1>行为分析</h1>{q.isLoading?<p>加载中…</p>:q.isError?<p role="alert">行为报表加载失败</p>:<ReportPrimitives result={q.data as never}/>}</section>}
