import {useQuery} from '@tanstack/react-query';import type {Phase35Api} from './api';import {ReportPrimitives} from './report-primitives';export function EmployeeReportPage({api}:{api:Phase35Api}){const q=useQuery({queryKey:['p35-report','employee'],queryFn:()=>api.read('/dashboard/reports/employee',{corpId:1,timezone:'Asia/Shanghai',startAt:'2026-01-01T00:00:00Z',endAt:'2027-01-01T00:00:00Z'})});return <section className="dashboard-content"><h1>会话分析</h1>{q.isLoading?<p>加载中…</p>:q.isError?<p role="alert">会话报表加载失败</p>:<ReportPrimitives result={q.data as never}/>}</section>}
