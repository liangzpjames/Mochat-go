import { useState } from 'react';
export function ReportFilters({ filters }: { filters: { startDate: string; endDate: string; employeeId: string; departmentId: string; setStartDate: (v: string) => void; setEndDate: (v: string) => void; setEmployeeId: (v: string) => void; setDepartmentId: (v: string) => void; apply?: () => void; reset?: () => void } }) {
  const [draft, setDraft] = useState(filters);
  return <form className="dashboard-filter-bar" onSubmit={(e) => { e.preventDefault(); filters.setStartDate(draft.startDate); filters.setEndDate(draft.endDate); filters.setEmployeeId(draft.employeeId); filters.setDepartmentId(draft.departmentId); filters.apply?.(); }}><label>开始日期<input type="date" value={draft.startDate} onChange={(e) => setDraft({ ...draft, startDate: e.target.value })} /></label><label>结束日期<input type="date" value={draft.endDate} onChange={(e) => setDraft({ ...draft, endDate: e.target.value })} /></label><label>员工<select value={draft.employeeId} onChange={(e) => setDraft({ ...draft, employeeId: e.target.value })}><option value="">全部员工</option><option value="1">当前授权范围</option></select></label><label>部门<select value={draft.departmentId} onChange={(e) => setDraft({ ...draft, departmentId: e.target.value })}><option value="">全部部门</option><option value="1">当前授权范围</option></select></label><button type="submit">查询</button><button type="button" onClick={() => { filters.reset?.(); setDraft(filters); }}>重置</button></form>;
}
