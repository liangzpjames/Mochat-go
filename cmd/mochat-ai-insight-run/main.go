// Command mochat-ai-insight-run is disabled: this process cannot authenticate
// a Dashboard principal, so it must perform zero database or model activity.
package main

import "log"

func main() {
	log.Fatal("mochat-ai-insight-run is disabled; use the authenticated Dashboard AI insight run endpoint")
}
